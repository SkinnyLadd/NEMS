spring:
  datasource:
    url: jdbc:postgresql://db.xcqvipaazevcqbclwtdx.supabase.co:5432/postgres?sslmode=require&currentSchema=nems
    username: postgres
    password: bgne123

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        type:
          preferred_enum_type: pgsql_enum

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE